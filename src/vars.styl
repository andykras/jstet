$main-color = red
$threshold-color = #bf3600
$vectorized-color = #ffa500
$scalar-color = #0071c5

$opacity = 0.7

opacity(n) {
    opacity: n < 1 ? (n > 0.2 ? (n - 0.2) : n) : n
    -webkit-opacity: n
}
